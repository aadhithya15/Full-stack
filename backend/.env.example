# â”€â”€ HueFit backend environment â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
# Copy this file to .env and fill in real values.
# .env is gitignored â€” NEVER commit real keys.

# Flask
FLASK_ENV=development
PORT=5000

# CORS â€” comma-separated allowed frontend origins
# Dev: Vite default. Prod: add your deployed frontend URL.
ALLOWED_ORIGINS=http://localhost:5173

# Uploads
MAX_UPLOAD_MB=5

# â”€â”€ Supabase (Phase 2) â€” dashboard â†’ Settings â†’ API â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
SUPABASE_URL=YOUR_SUPABASE_URL_HERE
SUPABASE_SERVICE_KEY=YOUR_SERVICE_ROLE_KEY_HERE
SUPABASE_JWT_SECRET=YOUR_JWT_SECRET_HERE

# â”€â”€ AI providers (Phase 5) â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
# Leave as placeholders for now. While placeholders are set, the API
# runs in MOCK MODE (returns sample recommendations, images still work
# via Pollinations which needs no key). Replace with real keys later â€”
# no code changes needed.
GEMINI_API_KEY=PLACEHOLDER_REPLACE_WHEN_AVAILABLE
GROQ_API_KEY=PLACEHOLDER_REPLACE_WHEN_AVAILABLE
